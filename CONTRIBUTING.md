# 参与贡献

## 添加课件

1. 根据课程代码找到 `courses/{小写课程代码}/slides/`。
2. 将 PPT、PPTX 或 PDF 文件放入该目录；存在多个版本时，按学期和教师或版本建立子目录。
3. 使用能说明内容的文件名，例如 `01-课程介绍.pptx`，避免 `新建演示文稿.pptx` 之类的名称。
4. 提交前确认文件可正常打开，且没有学号、手机号、私人账号、群聊截图等个人信息。

## 内容要求

- 只提交自己制作、已获作者授权或明确允许公开分发的课件。
- 不提交来源不明的整套课件、未公开试题、当前作业答案或带有敏感个人信息的文件。
- 不修改原课件来移除作者署名或版权说明。
- 相同课件不要重复提交；更新版本时应在提交说明中注明变化。

## 大文件

普通 Git 仓库不适合频繁修改超大二进制文件。单个文件较大时，应在附属仓库启用 Git LFS，并由仓库维护者统一配置跟踪规则。
